# 更新日志

本文件记录各版本汉化包的变动。版本号与对应的 OpenWorker 上游版本保持一致。

## v0.1.6（2026-07-26）

- 首个 Windows 中文汉化版本，对应 OpenWorker `v0.1.6`。
- DOM 级注入翻译引擎：在 Tauri WebView 初始化时注入，按词表实时替换界面英文为中文，覆盖侧边栏、设置、对话、自动化、连接器等主要界面。
- 独立补丁层设计：汉化与本体分离，本体升级后重新构建即可同步，已翻译内容不丢失。
- 一键构建脚本 `一键编译中文版.bat`：自动检测编译环境、克隆指定版本源码、注入汉化、复制后端 sidecar、编译并打包 NSIS 安装包。
- 语音输入自动降级：Windows 上 `whisper` 编译困难时自动切换为「关闭语音输入」版本，文字汉化不受影响。
- 发布安装包：`OpenWorker_0.1.6_x64-setup.exe`。
